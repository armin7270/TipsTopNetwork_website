<div dir="rtl">

# 🚀 راهنمای کامل دیپلوی و مهاجرت — TipStop Network

راهنمای قدم‌به‌قدم برای بالا آوردن سایت روی **Railway**، تمام تنظیمات لازم، و **مهاجرت بدون از دست رفتن اطلاعات** به اکانت جدید.

---

## 📋 فهرست

1. [دیپلوی روی Railway (از صفر)](#۱-دیپلوی-روی-railway-از-صفر)
2. [تنظیمات کامل Variables](#۲-تنظیمات-کامل-variables)
3. [اولین ورود و راه‌اندازی فروشگاه](#۳-اولین-ورود-و-راه‌اندازی-فروشگاه)
4. [سرویس‌های جانبی (Worker و Cron)](#۴-سرویس‌های-جانبی-worker-و-cron)
5. [اتصال دامنه اختصاصی](#۵-اتصال-دامنه-اختصاصی)
6. [مهاجرت به اکانت جدید Railway](#۶-مهاجرت-به-اکانت-جدید-railway)
7. [عیب‌یابی رایج](#۷-عیبیابی-رایج)

---

## ۱) دیپلوی روی Railway (از صفر)

### قدم ۱ — ساخت پروژه

1. وارد [railway.app](https://railway.app) شو (اکانت جدید = تریال رایگان)
2. **New Project** → **Deploy from GitHub repo** → ریپوی `TipsTopNetwork_website` را انتخاب کن
3. Railway خودکار `railway.json` و `nixpacks.toml` را می‌خواند و بیلد می‌کند (۲ تا ۴ دقیقه)

> ⚠️ اولین بیلد ممکن است به‌خاطر نبود متغیرها کرش کند — طبیعی است؛ بعد از قدم ۲ و ۳ درست می‌شود.

### قدم ۲ — اضافه کردن دیتابیس PostgreSQL (الزامی!)

**بدون این کار، با هر ری‌استارت همه تنظیمات پاک می‌شوند!**

1. روی همان صفحه پروژه (بوم گرافیکی) → دکمه **+ New** → **Database** → **Add PostgreSQL**
2. صبر کن دیتابیس سبز شود

### قدم ۳ — اتصال دیتابیس به وب‌سایت

1. روی سرویس **وب‌سایت** (کارت اصلی) کلیک کن
2. برو به تب **Variables**
3. این دو خط را اضافه کن (وقتی `${{` را بنویسی، خودش Postgres را پیشنهاد می‌دهد):

```
DB_CONNECTION=pgsql
DB_URL=${{Postgres.DATABASE_URL}}
```

4. Railway خودکار Redeploy می‌کند — صبر کن سبز شود

---

## ۲) تنظیمات کامل Variables

در سرویس وب → تب **Variables** → **+ New Variable**، این‌ها را یکی‌یکی اضافه کن:

### 🔑 اجباری

| نام | مقدار | توضیح |
|---|---|---|
| `APP_KEY` | `base64:....` | با دستور `php artisan key:generate --show` بساز. **این کلید را برای همیشه نگه دار** — رمز سرورها با آن قفل شده! |
| `APP_ENV` | `production` | محیط اجرا |
| `APP_DEBUG` | `false` | فقط موقع عیب‌یابی موقتاً `true` کن |
| `APP_URL` | `https://....up.railway.app` | آدرس عمومی سایتت (بعد از ساخت دامنه) |
| `DB_CONNECTION` | `pgsql` | نوع دیتابیس |
| `DB_URL` | `${{Postgres.DATABASE_URL}}` | اتصال خودکار به دیتابیس |

### 👤 پیشنهادی (برای ساخت اکانت مدیر در اولین بوت)

| نام | مقدار |
|---|---|
| `ADMIN_USERNAME` | `admin` |
| `ADMIN_PHONE` | `09120000000` |
| `ADMIN_PASSWORD` | رمز دلخواه قوی |

### 🧩 اختیاری (بسته به امکاناتی که فعال می‌کنی)

| نام | کجا استفاده می‌شود؟ |
|---|---|
| `DEPLOY_KEY` | یک رشته تصادفی طولانی — برای آدرس‌های `/deploy/status` و `/deploy/cron` و اکشن بکاپ خودکار |
| `STATE_KEY` | یک رشته تصادفی دیگر — برای رمزنگاری فایل state در مهاجرت خودکار |
| `NOWPAYMENTS_IPN_SECRET` | اگر از درگاه کریپتو استفاده می‌کنی |

> 💡 بعد از هر تغییر Variables، Railway خودکار Redeploy می‌کند.

---

## ۳) اولین ورود و راه‌اندازی فروشگاه

1. در Railway → سرویس وب → تب **Settings** → **Networking** → **Generate Domain** → آدرس عمومی سایتت ساخته می‌شود
2. آدرس را در متغیر `APP_URL` هم قرار بده و بگذار Redeploy شود
3. در Railway، روی سرویس وب → تب **... (منوی سه‌نقطه)** → **Terminal** را باز کن و اجرا کن:

```sh
php artisan db:seed --force
```

4. برو به `/login` و با `admin` / `admin` (یا مقادیر `ADMIN_*`) وارد شو
5. سایت مجبورت می‌کند رمز را عوض کنی — رمز قوی بگذار و **یادت نرود!**
6. از سایدبار ادمین → **🧭 راه‌اندازی** — چک‌لیست قدم‌به‌قدم را کامل کن:
   - 🖥️ افزودن سرور (3x-ui با مسیر مخفی، یا Marzban) → **بررسی اتصال** → ذخیره → **دریافت از پنل**
   - 🏷️ ساخت پلن فروش و اتصال اینباندها
   - 💳 ثبت شماره کارت‌ها در **تنظیمات**
   - 🤖 فعال‌سازی ربات تلگرام + ست کردن وبهوک
   - ⚡ (اختیاری) درگاه زرین‌پال / کریپتو / پیامک

---

## ۴) سرویس‌های جانبی (Worker و Cron)

این پروژه علاوه بر وب به **دو پردازش دیگر** نیاز دارد:

| سرویس | وظیفه | نحوه راه‌اندازی در Railway |
|---|---|---|
| **Worker** | ارسال برودکست تلگرام و جاب‌های صف | روی بوم → **+ New** → **GitHub Repo** → همان ریپو → بعد در Settings → Start Command را عوض کن به: `php artisan queue:work database --sleep=3 --tries=3 --timeout=900` |
| **Cron** | انقضای خودکار، سینک ترافیک، یادآوری، هلث‌چک سرورها، بکاپ | روی بوم → **+ New** → **Cron Job** → دستور: `php artisan schedule:run` و زمان‌بندی: `* * * * *` (هر دقیقه) |

> 💡 سرویس Cron و Worker هم باید همان Variables (به‌خصوص `DB_URL` و `APP_KEY`) را داشته باشند. راحت‌ترین راه: از گزینه **Copy service variables** یا لینک‌کردن Reference استفاده کن.

**بدون Cron:** انقضای خودکار و سینک ترافیک انجام نمی‌شود.
**بدون Worker:** پیام همگانی تلگرام ارسال نمی‌شود.

---

## ۵) اتصال دامنه اختصاصی

1. در Railway → سرویس وب → **Settings** → **Networking** → **Custom Domain**
2. دامنه‌ات (مثلاً `tipstop.ir`) را وارد کن؛ Railway دو مقدار CNAME به تو می‌دهد
3. در پنل DNS دامنه‌ات (Cloudflare و...) یک رکورد `CNAME` با آن مقادیر بساز
4. Railway خودکار SSL رایگان صادر می‌کند
5. متغیر `APP_URL` را به دامنه نهایی تغییر بده
6. وبهوک تلگرام را با دامنه جدید ست کن:

```
https://api.telegram.org/bot<TOKEN>/setWebhook?url=https://tipstop.ir/telegram/webhook
```

---

## ۶) مهاجرت به اکانت جدید Railway

چون تریال Railway محدود است، وقتی خواستی به اکانت جدید بروی، این مراحل دقیقاً به همین ترتیب:

### مرحله A — در اکانت فعلی (قبل از اتمام تریال!)

1. وارد **پنل ادمین** سایت شو → بخش **🚚 بکاپ و مهاجرت**
2. دو فایل را دانلود و نگه‌دار:
   - **💾 دانلود دامپ دیتابیس**
   - **📁 دانلود فایل‌های عمومی** (رسیدها و پیوست‌ها)
3. ⚠️ **خیلی مهم:** مقدار `APP_KEY` را از Variables کپی و در جای امن ذخیره کن. بدون این کلید، رمزهای ذخیره‌شده سرورها باز نمی‌شوند!

> 💡 بکاپ خودکار: هر هفته دامپ دیتابیس به تلگرام مدیر (چت‌آیدی تنظیم‌شده) ارسال می‌شود — پس حتماً ربات و چت‌آیدی را ست کن.

### مرحله B — در اکانت جدید

1. **New Project** → **Deploy from GitHub repo** → همان ریپو
2. **+ New** → **Database** → **PostgreSQL** (دیتابیس تازه)
3. در سرویس وب → Variables → **همه متغیرهای قبلی را عیناً وارد کن**، به‌ویژه:
   - `APP_KEY` — حتماً همان مقدار قبلی!
   - `DB_CONNECTION=pgsql` و `DB_URL=${{Postgres.DATABASE_URL}}`
4. Domain بساز و `APP_URL` را آپدیت کن
5. صبر کن اولین دیپلوی تمام شود — مایگریشن‌ها خودکار اجرا می‌شوند (دیتابیس جدید خالی است، طبیعی است)
6. وارد پنل ادمین شو (`admin` / مقدار `ADMIN_PASSWORD`) → بخش **🚚 بکاپ و مهاجرت**:
   - فایل **دامپ دیتابیس** را آپلود کن → «ورود دامپ»
   - فایل **فایل‌های عمومی** را آپلود کن → «ورود فایل‌ها»
7. سرویس‌های **Worker** و **Cron** را مثل بخش ۴ بساز
8. وبهوک تلگرام را با دامنه جدید ست کن
9. یک خرید آزمایشی انجام بده تا مطمئن شوی همه‌چیز سالم است ✅

**تمام!** همه کاربران، سفارش‌ها، موجودی کیف پول‌ها، تنظیمات و تاریخچه بدون حتی یک رکورد جاافتاده منتقل شده‌اند.

### 🤖 مهاجرت خودکار (اختیاری، پیشرفته)

می‌توانی بگذاری گیت‌هاب هر ۶ ساعت از سایت زنده **state رمزنگاری‌شده** بگیرد و داخل ریپو ذخیره کند؛ اکانت جدید موقع بوت خودکار آن را برمی‌گرداند:

1. در Railway اکانت فعلی، متغیرهای `DEPLOY_KEY` و `STATE_KEY` را ست کن
2. در گیت‌هاب → Settings → Secrets → این ۴ مورد را اضافه کن: `SITE_URL` ، `DEPLOY_KEY` ، `STATE_KEY` و (GITHUB_TOKEN خودکار است)
3. فایل آماده `.github/workflows/backup-state.yml` را فعال کن (schedule آن را باز کن)
4. در اکانت جدید همان `STATE_KEY` را ست کن — بوت خودکار `deploy-state/state.bin` را برمی‌گرداند

> ⚠️ این روش فقط دیتابیس SQLite را منتقل می‌کند؛ اگر روی Postgres هستی، روش دستی مرحله A کافی و مطمئن‌تر است.

---

## ۷) عیب‌یابی رایج

| مشکل | علت و راه‌حل |
|---|---|
| صفحه 500 روی همه صفحات | معمولاً `APP_KEY` ست نشده. متغیر را اضافه و Redeploy کن |
| 500 فقط صفحه اصلی | `APP_DEBUG=true` موقت بگذار، متن خطا را از تب Logs بخوان (بعد برگردان `false`) |
| خطای `Connection refused (5432)` | `DB_CONNECTION=pgsql` هست ولی Postgres وصل نیست — یا Postgres اضافه کن یا `DB_CONNECTION=sqlite` |
| تنظیمات بعد از مدتی پاک می‌شوند | روی SQLite موقتی هستی — حتماً Postgres وصل کن (بخش ۱ قدم ۲) |
| بنر زرد «دیتابیس موقتی» در پنل | همان مورد بالا — Postgres وصل کن |
| برودکست تلگرام ارسال نمی‌شود | سرویس Worker را نساخته‌ای (بخش ۴) |
| انقضای خودکار کار نمی‌کند | سرویس Cron را نساخته‌ای (بخش ۴) |
| `Cannot find native binding` موقع بیلد | کامیت قدیمی — Redeploy از آخرین کامیت |
| خطای `cURL 7` موقع تست پنل 3x-ui | پورت بسته یا IP/پورت اشتباه — فایروال سرور پنل را چک کن |
| خطای 403 موقع تست پنل 3x-ui | پنل‌های جدید 3x-ui — با آخرین نسخه کد، خودکار حل است |

---

## ✅ چک‌لیست نهایی قبل از فروش

- [ ] Postgres وصل است (`DB_URL`) و بنر زرد در پنل نیست
- [ ] `APP_KEY` در جای امن ذخیره شده
- [ ] `APP_DEBUG=false` است
- [ ] رمز `admin` عوض شده
- [ ] سرور پنل وصل و اینباندها ایمپورت شده‌اند
- [ ] پلن‌ها ساخته و به اینباند وصل شده‌اند
- [ ] کارت‌ها + ربات تلگرام + وبهوک ست شده‌اند
- [ ] Worker و Cron در حال اجرا هستند
- [ ] یک خرید آزمایشی کامل تا فعال‌سازی تست شده

</div>
